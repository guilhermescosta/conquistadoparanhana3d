using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Plow : MonoBehaviour
{
    public Material materialArable;  // material chão aravel
    public Material materialPlow;    // material chão arado

    bool isPlanted;   // informa se existe algum plantio no chão


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
